# UnoLock Agent Tests

Tests should be split between:

* `unit/`
  * narrow object and codec tests
* `integration/`
  * live local server interoperability tests

The first test work should remain integration-first against the local UnoLock server.
